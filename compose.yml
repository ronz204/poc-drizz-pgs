name: forger

include:
  - path: docker/postgresql/compose.yml

volumes:
  forger-postgres-data:
    driver: local
    name: forger-postgres

networks:
  forger-network:
    driver: bridge
    name: forger-network
